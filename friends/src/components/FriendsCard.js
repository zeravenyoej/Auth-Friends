import React from 'react';

const FriendsCard = (props) => {
    return (
        <div>
            {/* Have button to delete friends*/}
            DATA will go in here eventually
        </div>
    );
};

export default FriendsCard;